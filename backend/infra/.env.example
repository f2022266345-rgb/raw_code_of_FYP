# ── LUMINA shared infra env ─────────────────────────────────
# Copy to `.env` on the droplet (same directory as infra docker-compose.yml).
POSTGRES_USER=postgres
POSTGRES_PASSWORD=CHANGE_ME_strong_password
POSTGRES_DB=FYP_DB_Latest
